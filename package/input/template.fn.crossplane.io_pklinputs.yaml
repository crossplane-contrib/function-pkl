---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: pklinputs.template.fn.crossplane.io
spec:
  group: template.fn.crossplane.io
  names:
    categories:
    - crossplane
    kind: PklInput
    listKind: PklInputList
    plural: pklinputs
    singular: pklinput
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Input can be used to provide input to this Function.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              configMapRef:
                description: Load Project/Package from ConfigMap. Will evaluate PklProject
                  and *.pkl files within the ConfigMap.
                type: string
              inline:
                description: TODO implement
                type: string
              source:
                description: Source from which the Project is imported
                enum:
                - inline
                - configMap
                - uri
                type: string
              uri:
                description: Use URI Scheme to load Project/Package
                type: string
              xrdTemplate:
                description: Pkl Template of the CompositeResourceDefinition (XRD),
                  which will be amended by the CompositeResource (XR)
                type: string
            type: object
        type: object
    served: true
    storage: true
