---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: pkls.template.fn.crossplane.io
spec:
  group: template.fn.crossplane.io
  names:
    categories:
    - crossplane
    kind: Pkl
    listKind: PklList
    plural: pkls
    singular: pkl
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Input can be used to provide input to this Function.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              pklCRDs:
                description: Pkl Template of the CompositeResourceDefinition (XRD),
                  which will be amended by the CompositeResource (XR)
                items:
                  properties:
                    inline:
                      description: Contains a Pkl Template of a CRD
                      type: string
                    name:
                      type: string
                    type:
                      default: myDefaultValue
                      enum:
                      - inline
                      type: string
                  required:
                  - name
                  type: object
                type: array
              pklManifests:
                items:
                  properties:
                    configMapRef:
                      description: Load Project/Package from ConfigMap. Will evaluate
                        PklProject and *.pkl files within the ConfigMap.
                      properties:
                        name:
                          description: The name of the ConfigMap
                          type: string
                        namespace:
                          description: The namespace of the ConfigMap
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    inline:
                      description: Contains a stringified Pkl file
                      type: string
                    name:
                      type: string
                    type:
                      enum:
                      - uri
                      - inline
                      - configMap
                      type: string
                    uri:
                      description: Use URI Scheme to load Project/Package
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - pklManifests
            type: object
        type: object
    served: true
    storage: true
